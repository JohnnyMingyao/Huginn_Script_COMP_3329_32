using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlotcodeReset : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameSceneManager.Plotcode ="";
    }

    
}
